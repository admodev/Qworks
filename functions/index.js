const functions = require('firebase-functions');

// Placeholder endpoint for technical purposes.
exports.uploadFile = functions.https.onRequest((req, res) => {
  res.status(200).json({
    message: 'API invoked successfully!',
  });
});
